table:
  name: contact
  schema: public
array_relationships:
  - name: locations
    using:
      foreign_key_constraint_on:
        column: contact_id
        table:
          name: location_contact
          schema: public
